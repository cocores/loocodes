# Copy to .env.local (gitignored) for local dev, and add the same variable
# in Vercel Project Settings → Environment Variables for deploys.
# See web/README.md#google-maps-setup for how to create this key.
VITE_GOOGLE_MAPS_API_KEY=
